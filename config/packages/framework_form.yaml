framework:
    csrf_protection:
        enabled: true

    form:
        csrf_protection:
            enabled: true
            field_name: '%app.csrf_name%'
            
when@dev:
    framework:
        form:
            csrf_protection:
                enabled: true
            
