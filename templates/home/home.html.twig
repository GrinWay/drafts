{% extends 'base.html.twig' %}

{% block title %}
	{{ parent() }} Home
{% endblock %}

{% block body %}
	{{ dump(app.current_route, app.current_route_parameters) }}
	<br>

	{% set k = 'key_name' %}
	{% set v = 6 %}
	{% set result_array = 1..2 %}
	{% set result = 2 %}
	{% set start_date = date|date('Y') %}
	{% set end_date = start_date + 5 %}
	
	{{ include('twig/for_i.html.twig', {dopVar: 'bla'}, with_context=false) }}

	{#
	{{ dump(constant('class', hash_locator) is defined) }}
	{{ dump(date|for_user('+1200')) }}
	{{ dump(_context) }}
	{{ dump(_self) }}
	{{ dump(_charset) }}
	{{ attribute(hash_locator, 'flashes') is defined ? 'METHOD EXISTS' : 'METHOD DOESN\'T EXIST' }}
	#}
{% endblock body %}
