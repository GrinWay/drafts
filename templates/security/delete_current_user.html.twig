{% extends 'base.html.twig' %}

{% block title %}Delete account{% endblock %}

{#
1 _csrf
2 2 ошибки
#}

{% block body %}
    {{ form_start(form, { attr: {novalidate: 'novalidate'}}) }}
		{{- form_row(form, { row_attr: { class: 'my-0' } }) -}}
		
		<twig:grinway:btn class="btn-lg btn-outline-danger">
			Delete Account
		</twig:grinway:btn>
    {{ form_end(form) }}
{% endblock %}
