{% use 'bootstrap_5_layout.html.twig' with form_row as parent_form_row %}

{% block form_row %}
	{#
		Use the nesessary context:
	#}
	{% with { row_attr: { class: 'mb-1' } } %}
		{{ block('parent_form_row') }}
	{% endwith %}
{% endblock %}