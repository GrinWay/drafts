import { Controller } from '@hotwired/stimulus'
import 'chartjs-plugin-zoom'

/* stimulusFetch: 'lazy' */
export default class extends Controller {
	connect() {
		// AJAX for 'chartjs-plugin-zoom'
		//import('chartjs-plugin-zoom').then(() => console.log('zoom was loaded'))
		
		//const r = require('chartjs-plugin-zoom')
	}
}
